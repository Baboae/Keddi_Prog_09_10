    def get_age_by_breed(animals: list[object], breed_name) -> list[int]:
        agelst = list[int]
        for animal in animals:
            if not isinstance(animal, Animal):
                return False
            tolst = int(animal.get_age())
